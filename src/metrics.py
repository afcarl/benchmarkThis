"""
This is where all of benchmarking metrics will be stored

"""


def subcompositional_stress(X, Y):
    """
    A measure of subcompositional coherence

    Parameters
    ----------
    X : np.ndarray
        2-D array of compositions
        rows = compositions
        columns = components
    Y : np.ndarray
        2-D array of compositions
        rows = compositions
        columns = components


    Returns
    -------
    float : measure of stress
    """
    pass


def subcompositional_distance(X, Y):
    """
    A measure of subcompositional coherence

    Parameters
    ----------
    X : np.ndarray
        1-D array representing composition
    Y : np.ndarray
        1-D array representing composition

    Returns
    -------
    float : measure of mean squared distance
    """
    pass
